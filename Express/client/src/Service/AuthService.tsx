import axios from "axios";
/* 
const USER_API_BASE_URL = "http://localhost:5000/api/";

//inspired by https://www.devglan.com/react-js/react-js-jwt-authentication-example?fbclid=IwAR3DKBl2aT0f2LvBREQalGkrf9bX_7itVj_fJa69pt0bWx-Cf8tIi6WoWFM
class AuthService {
  login(credentials) {
    return axios.post(USER_API_BASE_URL + "generate-token", credentials);
  }

  getUserInfo() {
    return JSON.parse(localStorage.getItem("userInfo"));
  }

  getAuthHeader() {
    return { headers: { Authorization: "Bearer " + this.getUserInfo().token } };
  }

  logOut() {
    localStorage.removeItem("userInfo");
    return axios.post(USER_API_BASE_URL + "logout", {}, this.getAuthHeader());
  }
}

export default new AuthService(); */
